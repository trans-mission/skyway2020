{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run buildDev & npm start --inspect-brk=9229",
    "buildDev": "browserify index.js --debug -o static/bundle.js",
    "buildProd": "browserify index.js -o static/bundle.js",
    "watch": "budo index.js:static/bundle.js --live"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "p5": "^1.0.0",
    "tone": "^13.8.25",
    "watch": "^1.0.2"
  }
}
